"""
CounterFlow v1.0.0 — Database Migrations
==========================================
Currently unused — schema is managed by
SQLAlchemy create_all() at startup.

When a future release requires schema changes,
migration functions will be added here using
ALTER TABLE statements and version checks.
"""
